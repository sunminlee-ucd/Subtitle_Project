# Input subtitles

Place the `.srt` and timed `.csv` files that you want to translate in this folder.

Timed CSV files must contain `St` and `Et` columns plus a subtitle text column such as
`Subtitle_KO` or `Subtitle`.

Subtitle files placed here are intentionally ignored by Git.
